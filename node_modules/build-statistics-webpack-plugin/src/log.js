const debug = require('debug');
const { TRACE_NAME, ERROR_NAME } = require('./constant');

const trace = debug(TRACE_NAME);
const error = debug(ERROR_NAME);

module.exports = {
    trace,
    error,
};
