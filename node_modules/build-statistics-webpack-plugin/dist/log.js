"use strict";

var debug = require('debug');

var _require = require('./constant'),
    TRACE_NAME = _require.TRACE_NAME,
    ERROR_NAME = _require.ERROR_NAME;

var trace = debug(TRACE_NAME);
var error = debug(ERROR_NAME);
module.exports = {
  trace,
  error
};