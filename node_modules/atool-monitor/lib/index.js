'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.emit = emit;
var isAliEnv = require('is-ali-env');
var Reminder = require('update-reminder');

function emit() {
  isAliEnv().then(function (_isAliEnv) {
    if (!_isAliEnv) {
      return;
    }
    try {
      var reminder = new Reminder();
      reminder.report();
    } catch (_) {}
  });
}