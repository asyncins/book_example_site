import { ISatoriCreateBuild, IAdapter } from '../typings';
export default class SatoriAdapter implements IAdapter {
    private satoriClient;
    constructor();
    postBuild(args: ISatoriCreateBuild, _: object): Promise<void>;
    postDev(args: ISatoriCreateBuild, _: object): Promise<void>;
    uploadShameimaru(shameimaru: object): Promise<string>;
    uploadStatusFile(statsPath: string): Promise<string>;
    private ifSkipBuildReport;
    private ifDevReport;
    private saveFile;
}
